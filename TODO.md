# TODO 🚧

Your new site is all yours so it doesn't matter if you break it! Try editing the sketch.js file to add some text (p5.js' `text()` function) inviting the user to click it.

## Keep going! 🚀


<!-- https://cdnjs.cloudflare.com/ajax/libs/tone/15.5.36/Tone.min.js -->

Try adding more CSS properties so that the p5.js sketch appears within a margin.

Try adding another `div` (short for division) element underneath the `p` (short for paragraph) element. Give your new `div` an id property of "mySketch".

Add the p5.js library using a `script` tag in the page header.

Add a sketch.js file to your project and reference it from the head of the HTML file. Use the approach shown in Code 3.17 and available at https://cma4w.org/1/3/26 to have the sketch appear within the "mySketch" `div`.

Give your new `div` a class name and create a CSS element that specifies a margin for `div`s of this class. Now the sketch should appear within this margin.
